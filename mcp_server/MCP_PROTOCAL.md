# MCP Protocol

## vision check tool

> 该利用与视频框架交互，利用视觉模型检查生成动画的问题，以达到自动修复

接收参数，指定测试点id，返回JSON列表，每一个列表项代表一个待改进的问题以及对应的改进意见

```json
[
    {
        "bug": "发现AI模型的对话框，文字超出气泡宽度", // 这是问题
        "todo": "调整对话框宽度，确保文字不超出"         // 这是修改意见
    }
]
```
